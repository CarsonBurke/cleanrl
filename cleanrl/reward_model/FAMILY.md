# Reward Model

Auxiliary reward-model heads: predictive-state reward models (v1-v3) and the
innovation/Q-prop variant. Standalone ablations; world-model reward heads
live with their families (`lewm`, `../td-jepa/`, `../wm/`).
