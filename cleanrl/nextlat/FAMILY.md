# NextLat

Next-latent auxiliary line: action-conditioned dynamics models predicting
stopped online future latents (SmoothL1 + policy-space KL), distributional
Bellman variants, trust-region live heads, and prediction-Adam optimizers.
TPO-MD's `nextlat` threads stay in `../tpo/md/`; this directory holds the
standalone ablations.
