# Measurement

| File / configuration | Measurement / correction |
|---|---|
| `precision_audit.py`; 64 paired seeds resampling teacher, centres, stream, noise and init; LR grid 1.2e-5..1.97e-1 | All optima interior; Adam LR 9.6e-5, error 0.4022; extending the previous grid changed Adam error by -0.2204 ±0.0491; precision's previous 13% gain became +0.0938 ±0.0165 error versus Adam. |
| `novelty_stream.py`; init-only seeds, shared teacher/centres/stream, unused config `seed` | Seed SD 0.0203 versus task-resampled 0.0857; ratio 4.2x. |
| `precision_audit.py`; split one arm into disjoint halves, true gap 0, n=4 | Frequency of absolute gap ≥0.028: 65–77%; previous 4-seed gap -0.0276 and 8-seed gap +0.0329 used init-only sampling. |
| `hetvar_stream.py`; 8 paired task-resampled seeds, per-arm LR selection | Hetero 2: first Adam optimum 2e-5 at grid floor; expanded grid 2e-6..1e-3 placed optimum interior; batch 512 results also bracketed. |
| `hidden_stream.py`; 1024 inputs, 4 useful, noise 8, B=1 | Per-arm LR grid 1e-5..1e-2, no edge winners; ownfree/input/uniform/ownnull/credit results in `per_unit.md`. |
| `dense_boundary.py`; 8 paired seeds | Per-arm LR winners interior; density and switch results in `per_unit.md`. |
| `pc_stream.py`; field 1.0, 16 paired seeds | Per-arm LR grid starts at 3e-5; upper endpoint and selected-LR edge status not recorded. |
| `stock_stream.py --method hetvar --raw-target --vol-feature` | LR grid 1e-6..3e-5; winners interior; Adam 0.99941, hetvar 0.99938. |
| `sample_stream.py`; corrected selection and noise | Separate train/validation/test RNG namespaces; select LR by sustained clean-validation checkpoints before test; sustained, segment, endpoint and predict-before-update clean error reporting; previous same-test selection/endpoint-only switch measurements replaced; df=2 previously had zero noise, now explicit CUDA-generator Student-t noise with nonzero scale for df≤2. |
| `sample_stream.py`; jobs 5294/5295, 10 arms ×11 LRs ×65,536 observations | All selected LRs interior; df=2+switch / hetero=2+switch wall time 32.35/18.94 s, setup 15.84/2.28 s, updates ~15.1 s. |
| `noisy_stream_diagnostic.py`; corrected stream protocol | Common train/evaluation streams across arms; independent twin RNG; past-only evidence; replica-local calibration; moving/returning multi-input support; exact counts and Bernoulli clean-risk decomposition into signal reconstruction, distractor leakage and signed mean cross term. |
| `noisy_stream_diagnostic.py`; mirror division, job 5292 | Equal one-observation ratios computed as 0.99999994 versus 1.0 changed tail counts 3/5 versus 1/3; corrected with `tl.div_rn` and squared statistics; compiled capture/uncaptured recurrences bit-identical; corrected recovery job 5336 in `sparse_signal.md`. |
| `/tmp/channel_probe.py`; requested multiplier 0.125, Adam beta1=0.9 | Pre-Adam: realized step 0.897 after 1 batch, 0.594 after 10, →1.00 sustained; post-Adam: 0.124/0.124/0.125. |
| `hetvar_stream.py bins`; pre- versus post-Adam | Pre-Adam level 0.005 absorbed more noise than Adam; post-Adam cell results and level 0.001 in `per_unit.md`. |
| Cumulative evidence batching | Historical formula `t=sqrt(n)*mu/sigma` replaced by `t=sqrt(n)*mu/sqrt(sigma²+B*mu²)`; verified within 2.2% at B={1,8,64,1024}; attenuation `1/sqrt(1+B*mu²/sigma²)`; PPO sigma²/mu²=800–8000, B=32768 gives 0.15 at 800. |
| `hidden_stream.py`; 4/1024 useful, noise_std 8, 60,000 samples, steps=samples/B, own LR per arm | B=1: Adam/mirror_input/oracle 0.6703/0.3266/0.3205; B=8: 0.6384/0.3134/0.2743; B=64: 0.7287/0.3243/0.2803; B=256: 0.8137/0.3458/0.3151. |
| Same 60,000-sample batch sweep | B=1 mirror versus minimum Adam across batches: 1.96x; mirror range 0.313–0.346; rounded Adam B=1→256 values 0.670→0.814. |
| `pc_stream.py`; batch update correction | `u += (pc_lr / batch) * du`, with batch-summed du, replaced by `u += pc_lr * du`; B=1 bit-identical; old B=256 multiplier/dispersion 0.982/0.020 versus B=1 0.728/0.424; corrected dispersion B=1/32/256: 0.423/0.429/0.412. |
| `cleanrl/shared/state_plasticity.py`; mirror | `A=sum(g)`, twin `R=sum(eps*g)`, level `1-FDP`; 16/16 contracts in `/tmp/verify_mirror.py`. |
| `hidden_stream.py`; scalar and twin controls | `lr_eff(i)=L*G(t)*c_i`; uniform uses only mean(c); ownfree/ownnull use own twin, input/credit use layer twin; controls independently LR-selected. |
| `ppo_continuous_action_pcbatch_v1.py`; control arms | `--pc-off`: ungated; `--pc-scalar`: per-unit mean with sample dependence removed; `--pc-shuffle`: same gate marginals with correspondence removed. |
| `unit_bayes_stream_v1.py`; shuffle correction | Fixed permutation plus inverse posterior routing reproduced neuron arm to roundoff; corrected control keeps each unit's covariance history and applies another unit's operator; sustained neuron/shared/shuffle/scalar 0.166467/0.173668/0.176861/0.170189 (5286). |
| `stock_stream.py`; twin calibration | Per-step shared/independent-coordinate signs at frozen weights: null level 0.696/0.690; causal centring: real/permuted 0.828/0.344; synthetic null 0.008. |
| `hetvar_stream.py` / `stock_stream.py`; heteroscedastic twin | Mispair residual² with random past state using past-sample ring, rather than lag-1; SPY c_sd 0.36→0.70; fixed decay 0.3 removed 60% of gain; decay 0 added +0.018 on null; fixed nu=5 added +0.005 Gaussian error, adaptive nu -0.0003. |
| `scripts/benchmark_plasticity_proxies.py`; job 5293, 4 rotating-order repetitions, 256 updates, all 110 candidates | Median eager/compiled/graph 1.499/0.312/0.0586 s; graph/eager speed ratio 25.56x; warm-cache setup 2.22 s; uncaptured compiled/graph bit-identical; eager/compiled nonfinite disagreements 0, per-LR trajectory drift recorded in `benchmarks/plasticity/proxy_throughput.json`. |
| `noisy_stream_diagnostic.py`; execution rewrite | Previous ~6 host synchronizations/step; 20k steps 11 s versus ~1 s; independent seeds batched, 20k×16-seed run takes seconds. |
| CUDA verification; queue `maxParallelRuns=1` | Legibility 3 analytical checks (5271); block covariance 6 checks (5260, attempt 2); full covariance 5 (5296); meta-descent 6 (5279); sparse 19 (5272, attempt 2); integrated 39 passed, 0 failed/skipped, 7.73 s (5344). |
| `optimizer_proxy_eval_v8.py`; jobs 5881/5882/5889/5890 | Match LR, beta1, beta2, decay and output-head LR searches; noisy validation locks complete trajectories before test scoring. Online regression, moving targets with eight-pass reuse, and signed clipped-policy objectives expose the old transport proxy's transfer gap. Adaptive development, not untouched holdout. |
| `optimizer_proxy_confirm_v1.py`; job 5895 | Freeze configurations before new seed-1 RNG namespaces generate teachers/init/train/test data. All 15 full trajectories completed; five-step polar risk -16.77%/-7.10%/-8.99% versus tuned AdamW, training cost 2.32–2.69x. Sixteen-checkpoint right-endpoint quadrature, no seed-level significance or equal-compute/PPO claim. |
| `optimizer_proxy_v8_contracts.xml`; job 5880 | 34 CUDA contracts passed, zero failures/errors/skips. Confirmation analysis additionally verified 10 recorded source hashes, 240 checkpoint files and all full-horizon update counts. Failed polar-eight online capture (5883) remains visible, not hidden by candidate masking. |
| `predictive_optimum_v1_contracts.xml`; job 6125 | 23 corrected v5 sparse/v4 panel contracts passed. Earlier trace-index alias (6116) and preprocessing-auth fixture (6118) failures remain recorded; their dependent learning jobs did not run. |
| `return_representation_v5_contracts.xml`; job 6137 | 21 direct-return numerical/causal contracts passed. Actual uncentered raw-zero baseline, current-denominator unit conversion, auxiliary gradient coupling and signed-label null controls. |
| `predictive_iteration_v6_verified_contracts.xml`; job 6148 | 33 contracts passed at unchanged tolerances after excluding beta2=0 before learning. The failed probe (6140: 32 passed, 1 failed; maximum weight difference .000155) remains in `return_beta2_zero_v6_probe.py` and its original XML. No claim that the beta2=0 mathematical update was disproven. |
| `structural_inference_v7_verified_contracts.xml`; job 6183 | 44 numerical, causality, graph-restoration and posterior contracts passed, zero failures/errors/skips. Independent static sparse and financial reviews found no concrete defect; reviews did not rerun tests or raw-array audits. Across accepted jobs6125/6137/6148/6183: 121 passing test instances, not 121 independent scientific replications. |
| Sparse v7, fresh jobs6199–6203 | All five streams reach 60,000 observations and 7,680,000 resampling uniforms with unchanged source/data/lock hashes. Exactness is within represented singleton segments, not the retained history distribution or factorized hedge. Change and recurrence fail the prelocked 20% promotion target. |
| Return v6, jobs6154/6163/6168 | Every candidate is recorded; both auxiliary families select weight zero. Raw forecasts use known RMS through t, with genuine raw-zero comparison. Signed-null labels preserve raw/normalized energy. Rank400 [90145,101182) is new versus the cited v3 consumption, not pristine market dates. |
| Panel interpretation | Latest t…t-31 versus old t-1…t-32 is an information change. Existing Bank already has volume surprise. Coverage filtering can bridge gaps; survivor-selected cohorts and the unenforced `first_session_before` argument limit historical as-of claims. Selection remains normalized-prefix MSE, even when reporting raw-return MSE. |
| Hierarchy v7, jobs6189/6217–6219 | All 192 structural candidates stay finite; three independent end-state coefficient audits agree in each full-history run. Nevertheless, locked hierarchy raw MSE is 2283.78x zero on rank600. Numerical correctness is not statistical adequacy; no promotion or silent fallback. |
| Development magnitude diagnostic, job6220 | Reconstructs raw units and matches recorded counts/target/prediction energies. Maximum absolute feature182321.55; 255/8799705 samples above1000 account for79.65% of hierarchy prediction energy. Magnitude bins are not formal posterior leverage or proof of the additional-cohort failure's cause. |
