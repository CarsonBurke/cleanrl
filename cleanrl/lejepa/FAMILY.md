# LE-JEPA

Latent-embedding predictive (JEPA-style) auxiliaries for continuous control:
embedding-policy, norm-shell, risk-trace, geometric/geodesic and AWR variants,
plus the PAN+LE-JEPA direct port. Geometric actor variants live in
`../encoder-value/`, TD-JEPA in `../td-jepa/`, TD7 LE-JEPA ports in
`td7/{lesale,stocksig}/` — cross-linked, not consolidated.
