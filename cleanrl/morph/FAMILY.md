# Morphogenic Compute

State-dependent computation substrates instead of fixed actor/critic MLPs:
learned growth/shrink pressure, coordinate-field routing, plasticity, and
adaptive compute budgets multiply the PPO losses by a differentiable compute
estimate.

- `compute/` — morphogenic-compute line (v1-v25).
- `sym/` — morphogenic-symmetry line (v1-v13).
